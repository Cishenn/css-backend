<?xml version="1.0" encoding="utf-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.cishenn.ccs.dao.TagMapper">
    <insert id="save" parameterType="com.cishenn.ccs.entity.Tag">
        insert into tag(tag_id, tag_name, description, type, creator_id, create_time)
                    values(#{id}, #{name}, #{desc}, #{type}, #{creator_id}, #{create_time})
    </insert>
</mapper>